const { usersCont, usersWorker } = require("./test");
const { visitorsCont, visitorsWorker } = require("./visitors");
module.exports = {
  usersCont,
  usersWorker,
  visitorsCont,
  visitorsWorker
};
