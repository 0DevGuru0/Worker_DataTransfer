const { usersCont, usersWorker } = require("./users");
const { visitorsCont, visitorsWorker } = require("./visitors");
module.exports = {
  usersCont,
  usersWorker,
  visitorsCont,
  visitorsWorker
};
